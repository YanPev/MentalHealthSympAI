"""Presentation-quality figures for the final PHQ-8 deck.

Each ``figN_*.py`` reads only existing artifacts under ``outputs/`` (no model
training) and emits a 300-dpi PNG plus a companion CSV/JSON to ``outputs/figures/``
so every number on a slide is reproducible.
"""
